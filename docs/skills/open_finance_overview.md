# Open Finance Overview

- This app uses the local mock Open Finance MCP server, not public Belvo REST APIs.
- Available MCP tools are `get_owners`, `list_accounts`, and `list_transactions`.
- The data boundary is read-only.
- The demo dataset represents one synthetic user with accounts and processed transaction history.
- Financial answers should be grounded in MCP tool results and deterministic calculations.
